spring:
  cloud:
    openfeign:
      client:
        config:
          default: # 默认配置
            logger-level: full
            connect-timeout: 1000
            read-timeout: 2000
#          service-product: # 指定服务名 @FeignClient("service-product")
#            logger-level: full
#            connect-timeout: 3000
#            read-timeout: 5000